import javax.swing.JFrame;

public class Main extends JFrame {
    public static void main(String[] args) {
        Checkbox_ ch = new Checkbox_();
        ch.setDefaultCloseOperation(EXIT_ON_CLOSE);
        ch.setSize(300, 200);
        ch.setVisible(true);
    }
}
